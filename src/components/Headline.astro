---
const { tag = 'h2', margin = 'default' } = Astro.props

const Tag = tag

let classes =
  'font-extrabold text-primary text-2xl md:text-3xl lg:text-4xl leading-relaxed '

if (margin === 'small') {
  classes += 'mb-4'
} else {
  classes += 'mb-8'
}
---

<Tag class={classes}><slot /></Tag>
